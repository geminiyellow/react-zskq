// 常量

import keyMirror from 'fbjs/lib/keyMirror';

module.exports = {
  // 考勤地址距屏幕左边距离
  PADDING_LEFT_WIDTH: 18,
  // Event Listener Identifier.
  event: keyMirror({
    SU_SCAN_DISABLED: null,
  }),
};