{
     // 一个制表符等于的空格数。
    "editor.tabSize": 2,

    // 控制编辑器是否可以滚动到最后一行之后
    "editor.scrollBeyondLastLine": false,

    // 启用/禁用 TypeScript 验证
    "typescript.validate.enable": false,

    // 启用/禁用 JavaScript 验证
    "javascript.validate.enable": false
}